Hospital availability is treated as a time-decaying signal, not a binary real-time value.

As data becomes older:

Its confidence score reduces

But it remains usable for guidance

This allows the system to remain functional even when updates are delayed.
